- [[GPT 5.1]]
- [[Sonnet 4.5]]
- [[Sonnet 4.5]]

`````markdown
Сгенерируй мне на английском языке системный промпт для реализации аналога персонального deepresearch агента на основе claude code ai. Ориентированного на конкретного пользователя. В качестве доступа к интернету будет доступен `curl`, а для более сложных случаев предоставлен puppeteer в качестве mcp.

Анализ будет состоять из нескольких фаз. Для каждой из фаз агенту разрешено и даже необходимо:

- ВАЖНО: должен быть использован придуманный в начале `<research-id>` - короткий, понятный и уникальный в пределах `./research`. Должен быть консистентным, не меняться в процессе исследования.
- Проверять актуальность знаний в интернете
- Получать необходимые данные из интернета
- Записывать необходимые файлы в папку `./research/<research-id>/knowledge` - для данных нужных далее в формате markdown, `./research/<research-id>/tmp` - все временные и побочные файлы, скрипты и прочее что необходимо для выполнения задачи
- Для всех файлов в формате markdown файлы должны быть ориентированны на Obsidian. А также:
    - Оберни все энтити, имена файловой системы, экспрешены программирования в одинарные специальные апострофы - `(например`unsafe`или`object.call()`), если они отсутствуют. В том числе в заголовках. Не путай специальный апостроф с одинарной кавычкой.
    - Пометь все многострочный код, примеры и т.д. в тройные апострофы (```), если они отсутствуют
    - Определи и добавь многострочному коду после открывающих апострофов язык, на котором написан пример или код, если актуально.
    - Текст не используемый в примерах и т.д. сделай в профессиональной типографии в соответствии с тем языком на котором он написан. Особое внимание удели кавычкам и тире, дефис и т.д.
    - Можно использовать также расширения Obsidian: table, диаграммы `mermaid`, latex формулы и т. д.

При использовании claude code в режиме plan мы будем составлять план из пунктов и в формате markdown `- [ ] ...` и записывать план в файл с расширением `.md` в папку `./research/<research-id>/plan.md` План должен быть достаточным и подробным для реализации его агентом step-by-step. При написании плана, следуй шагам:

- Проанализируй запрос и определи главную цель и полноту информации
- Составь список вопросов на которые необходимо ответить перед составлением плана
- Уточни нужную информацию у пользователя или в интернете
- Проанализируй полученную информацию и составь черновик плана
- Проведи непредвзятую оценку качества плана и в соотвествии с запросом как незаинтересованный сторонний аудитор
- Внеси изменения в план согласно оценке из преведущего шага

В режиме редактирования claude code выполняет соответствующий план шаг за шагом пока не выполнит все шаги и не достигнет конечного результата. После выполнения каждого выполненного шага агент помечает в файле плана пункт как выполненный `- [x] ...`, А под ним должен быть комментарий о проделанной работе и ссылки на локальные файлы в папке `./research/<research-id>/knowledge` если таковые были созданы. Чтобы можно проследить всю цепочку, и в любой момент продолжить если процесс был прерван.

Важные контекст:

- Побудь моим ментором высокого уровня, с большим опытом, и современными практическими знаниями. Помоги мне с вопросом:

````markdown
Сгенерируй мне на английском языке системный промпт для реализации аналога персонального deepresearch агента на основе claude code ai. Ориентированного на конкретного пользователя. В качестве доступа к интернету будет доступен `curl`, а для более сложных случаев предоставлен puppeteer в качестве mcp.

Анализ будет состоять из нескольких фаз. Для каждой из фаз агенту разрешено и даже необходимо:

- ВАЖНО: должен быть использован придуманный в начале `<research-id>` - короткий, понятный и уникальный в пределах `./research`. Должен быть консистентным, не меняться в процессе исследования.
- Проверять актуальность знаний в интернете
- Получать необходимые данные из интернета
- Записывать необходимые файлы в папку `./research/<research-id>/knowledge` - для данных нужных далее в формате markdown, `./research/<research-id>/tmp` - все временные и побочные файлы, скрипты и прочее что необходимо для выполнения задачи
- Для всех файлов в формате markdown файлы должны быть ориентированны на Obsidian. А также:
    - Оберни все энтити, имена файловой системы, экспрешены программирования в одинарные специальные апострофы - `(например`unsafe`или`object.call()`), если они отсутствуют. В том числе в заголовках. Не путай специальный апостроф с одинарной кавычкой.
    - Пометь все многострочный код, примеры и т.д. в тройные апострофы (```), если они отсутствуют
    - Определи и добавь многострочному коду после открывающих апострофов язык, на котором написан пример или код, если актуально.
    - Текст не используемый в примерах и т.д. сделай в профессиональной типографии в соответствии с тем языком на котором он написан. Особое внимание удели кавычкам и тире, дефис и т.д.
    - Можно использовать также расширения Obsidian: table, диаграммы `mermaid`, latex формулы и т. д.

При использовании claude code в режиме plan мы будем составлять план из пунктов и в формате markdown `- [ ] ...` и записывать план в файл с расширением `.md` в папку `./research/<research-id>/plan.md` План должен быть достаточным и подробным для реализации его агентом step-by-step. При написании плана, следуй шагам:

- Проанализируй запрос и определи главную цель и полноту информации
- Составь список вопросов на которые необходимо ответить перед составлением плана
- Уточни нужную информацию у пользователя или в интернете
- Проанализируй полученную информацию и составь черновик плана
- Проведи непредвзятую оценку качества плана и в соотвествии с запросом как незаинтересованный сторонний аудитор
- Внеси изменения в план согласно оценке из преведущего шага

В режиме редактирования claude code выполняет соответствующий план шаг за шагом пока не выполнит все шаги и не достигнет конечного результата. После выполнения каждого выполненного шага агент помечает в файле плана пункт как выполненный `- [x] ...`, А под ним должен быть комментарий о проделанной работе и ссылки на локальные файлы в папке `./research/<research-id>/knowledge` если таковые были созданы. Чтобы можно проследить всю цепочку, и в любой момент продолжить если процесс был прерван.

Отвечай подробно, по возможности без аналогий, как человеку знакомому с предметом, но без реальной практики. При ответах опирайся на самые актуальные данные. Сверяйся с интернетом. Согласуй свои ответы с опытом реальных пользователей, трендсеттерами и стандартами в отрасли.

При составлении системного промпта:

- Добавь все необходимые ещё фазы и пункты для эффективного ресерча
- Сделай так чтобы использовалася самая длинная из цепочек рассуждения
- Требования перепроверялись после выполнения
- Аналитически подход достигал уровня лучших аналитиков в теме запроса
- Системный промпт должен ариентироваться на максимальную эффективность для больших языковых моделей, в частности для утилиты `сlaude code`
- Колличество токеннов имеет значение, составь промпт максимально эффективно в соотношении "количество токенов"/"эффективный результат"
````
`````

```

```
